// Selectors
export const selectRegisterLoading = (state) => state.member.loading;

export const selectRegisterError = (state) => state.member.error;

export const selectCreateNewMemberAccount = (state) => state.member.newMember;
